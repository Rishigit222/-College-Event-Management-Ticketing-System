# College Event Management Ticketing System

Simple C-based ticketing system for college events.

## Files
- `ticketing system.c` — main source file (C)

## Build & Run
This project currently contains a single C source file. To compile and run (Windows with GCC/MinGW or WSL):

# Using MinGW (PowerShell)
# Install MinGW and add `gcc` to PATH, then run:
# ```pwsh
# gcc "ticketing system.c" -o ticketing_system.exe
# .\ticketing_system.exe
# ```

# Using WSL or Linux
# ```bash
# gcc "ticketing system.c" -o ticketing_system
# ./ticketing_system
# ```

## Author
- Rishi

## License
Add a license file if you want to make this project open source.

## Notes
- This README was added automatically. Update it with build instructions, dependencies, and usage examples as you develop the project further.
